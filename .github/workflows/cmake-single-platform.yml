# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: Sideria Audio Lib Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Debug

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: 'true'

    - name: Install gnuplot
      # Configure CMake in a 'build' subdirectory.
      run: brew install gnuplot

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory.
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Debug

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build --config Debug --target tests

    - name: Test
      working-directory: ${{github.workspace}}/build/siderialib/tests
      run: ./tests

    - name: Upload Plot Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: plots
        path: |
          ${{github.workspace}}/build/**/*.svg
